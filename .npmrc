# pnpm 配置文件
# 启用严格模式，防止访问未声明的依赖
strict-peer-dependencies=true

# 自动安装 peer dependencies
auto-install-peers=true

# 使用精确版本锁定
save-exact=false

# 启用 pnpm 的快速安装
prefer-frozen-lockfile=true

# 设置 registry（可选，使用默认的 npm registry）
# registry=https://registry.npmjs.org/ 